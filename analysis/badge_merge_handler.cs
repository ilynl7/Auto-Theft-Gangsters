using Sproto;
using SprotoType;

public class badge_merge_handler
{
	public static void badge_merge_response(SprotoTypeBase rep)
	{
		badge_merge.response response = rep as badge_merge.response;
		if (rep == null)
		{
		}
	}
}
