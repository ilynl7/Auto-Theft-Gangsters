using Sproto;
using SprotoType;

public class character_pick_handler
{
	public static SprotoTypeBase character_pick_request(SprotoTypeBase req)
	{
		character_pick.request request = req as character_pick.request;
		if (request != null)
		{
		}
		return null;
	}
}
